对象存储都采用obj方式，方便读取，不易出错。
网络传输尽量采用数组传输，字节少。
角色行走，采用向量表示方向，非准确位置，一旦接收数据顺序颠倒结果也是对的，或者丢失其中一个数据，后续的操作依然继续。
八方向顺序：上开始顺时针旋转。
本地存储尽量用对象，操作方便并且快，网络传输用数组，节省流量。
所有id均从0开始设计（特殊的除外）。另需要代码单独实现其中很多类型的，宏定义后在进行代码编写。


攻击移动完后静止一下再切换成idle状态，防止出现idle太短的情况。